/*
    @Return void
    @Param tableID - The ID of the reselts table being populated (what needs to be made visible after populating)
    @Param tableBodyID - The ID of the body of the table being populated (where results are added)
    The methods adds results from a database to a results table on the search page.
*/
function populateResults(tableID, tableBodyID)
{
    // Code to create and add results to the results table (tableBodyID)

    // Code to make table visible after adding results to it (tableID)
}
